# la imagen base que se nos propone
FROM php:7.4-apache
# coloca en el directorio raíz del servicio web los ficheros creados
COPY ./web/ /var/www/html/
# se utiliza el puerto 80 por defecto
EXPOSE 80